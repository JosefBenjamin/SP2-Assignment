public class Main {

    /*All applications made in Java must have a public Main.java file in the project repository,
    So that the Java application can run, so even though this class is empty with only a main method, it's sole purpose
    is to make sure the code can compile and we don't get a runtime/compile error.
     */

    public static void main(String[] args) {


    }
}
